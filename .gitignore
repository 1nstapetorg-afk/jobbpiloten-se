# Round-77-post init — top-level .gitignore
node_modules/
.next/
out/
*.lock
extension/build/
extension/__lint_await_async_negative_test__.js
dev-*.log
/tmp/
.DS_Store
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
dist/
test-results/
playwright-report/
# Round-94 followup — Lighthouse CI local report output
.lighthouseci/
extension/build-config.json
# Round-88 — Chrome Web Store upload archive (build artifact)
extension-v1.0.0.zip

# Round-89 (P2 #9) — legacy cleanup artefacts (dashboard page backups,
# full-repo snapshots, session scratch files)
*.bak-final
*.bak
jobbpiloten-complete-backup.zip
last_response.txt

# 2026-07-21 / Round-72.2 / Followup-3 — runtime artifacts from
# scripts/run-dev.mjs that should never be committed. The dot-prefix
# means they don't match the existing dev-*.log glob (which requires
# the filename to start with dev-, no leading dot).
.dev-server.log
.dev-server.pid

# Allow .env.template (placeholders, NEVER real secrets)
!.env.template

next-env.d.ts
tsconfig.json
